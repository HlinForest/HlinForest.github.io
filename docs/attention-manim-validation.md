# ManimGL 图解发布验收（2026-09-09）

## 交付范围

- 原有 38 个主题图组替换为 164 个分步关键帧。
- 桌面与手机各 164 张 PNG，共 328 张。手机重新排列完整对象，
  流程箭头朝下；仅裁掉画布外围空白，不裁断或拼接文字和矩阵。
- 保留原文章 URL、数学定义、附录代码和旧 SVG 资源地址。
- 源码、渲染入口、数值检查位于 scripts/attention-manim；
  场景公式、数值、图注和图片校验和包含在两份公开生成清单中。
- 所有图内数学除法使用 LaTeX 分式，Python 除法语法保留。

## 数学与源码

- check_scenes.py：38 组覆盖完整，164 个场景，115 个可直接数值化的
  矩阵乘法、加减或逐元素乘法独立复算通过。
- attention-check.py：附录 A/C 与下载代码一致；328 张响应式 PNG 引用完整；
  逐步描述与源场景一致；图片 SHA-256 和源码哈希通过。
- 源码哈希同时提供正规化 LF 版本，兼容 Git 的 LF/CRLF 换行转换；
  图片哈希始终逐字节校验。
- 原参考程序：129 项 CPU 检查通过；变体程序：18 项通过。
- PyTorch SDPA CPU 对照通过；环境无 CUDA，官方 FA2/FA3 GPU 检查跳过。
- 各组局部例子不是同一套端到端模型参数。示意 latent、另取概率行、
  非负特征零分量及简化门参数的限制写在相应图注中。

## 图形检查

- 浏览全部桌面关键帧总览，并放大检查代表性的分式、负数、状态链、
  head 权重行块、DSA gather 和手机版图。
- 修正发现的 LaTeX 字符串转义、负零显示、零值填色、KV 分块颜色、
  桌面运算符基线及手机箭头方向问题。
- 渲染器检查标题、公式、单元格文字和整体布局的边界。
- 同形矩阵使用同一单元格边长；转置真实交换长宽；拼接和权重分块
  在实际格子边界处标记。已知零和不可见位置留白。

## 网站

- pnpm build：Astro 检查与静态构建通过。
- node scripts/check-site.mjs：5 个 HTML 页面、465 个生成文件，
  无断开的站内引用。
- Edge/Playwright 检查 1440×900 与 390×900：
  每种视口均有 38 组、164 张步骤图；无缺图、错误布局选择、
  KaTeX 错误、图片越界或整页横向溢出。
- 检查前等待全部图片解码，避免懒加载过程被误判为缺图。
- 截图和运行日志保存在本地 tmp/manim-review 与 tmp/attention-*.log，
  不随网站发布。

GitHub Pages 部署结果以提交后的工作流状态为准。
